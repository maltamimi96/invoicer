import { ScheduleView } from "@/screens/ScheduleView";

/** Admin schedule tab — same day-grouped view the worker group renders. */
export default function ScheduleTab() {
  return <ScheduleView />;
}
